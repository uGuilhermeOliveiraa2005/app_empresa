import 'package:flutter/material.dart';

class AppColors {
  // Laranja principal (Vibrante)
  static const Color primary = Color(0xFFFF6600);
  
  // Laranja clarinho (Para fundos de detalhes)
  static const Color primaryLight = Color(0xFFFFE0B2);
  
  // Branco (Para o fundo geral)
  static const Color background = Colors.white;
  
  // Cinza escuro (Para textos importantes)
  static const Color textDark = Color(0xFF2D2D2D);
  
  // Cinza claro (Para textos secundários)
  static const Color textLight = Color(0xFF757575);
  
  // Vermelho (Para erros ou botão de cancelar)
  static const Color error = Color(0xFFD32F2F);
}